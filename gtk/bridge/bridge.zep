// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace Gtk\Bridge;

class Bridge
{
    public static function init() -> bool
    {
        int r;

        let r = bridge_init();

        return r == 1;
    }

    public static function retain(int handle) -> bool
    {
        int r;

        let r = bridge_retain(handle);

        return r == 1;
    }

    public static function release(int handle) -> void
    {
        bridge_release(handle);
    }

    public static function isValid(int handle) -> bool
    {
        int r;

        let r = bridge_is_valid(handle);

        return r == 1;
    }

    public static function typeName(int handle) -> var
    {
        var result;

        let result = bridge_type_name(handle);

        return result;
    }

    public static function isA(int handle, string typeName) -> bool
    {
        int r;

        let r = bridge_is_a(handle, typeName);

        return r == 1;
    }

    public static function typeFromName(string typeName) -> int
    {
        return bridge_type_from_name(typeName);
    }

    public static function pump(int timeoutMs) -> int
    {
        return bridge_pump(timeoutMs);
    }

    public static function connect(int handle, string signal, var callback) -> int
    {
        return bridge_connect(handle, signal, callback);
    }

    public static function disconnect(int handle, int handlerId) -> void
    {
        bridge_disconnect(handle, handlerId);
    }

    public static function getProperty(int handle, string name) -> var
    {
        var result;

        let result = bridge_get_property(handle, name);

        return result;
    }

    public static function setProperty(int handle, string name, var value) -> void
    {
        bridge_set_property(handle, name, value);
    }
}
