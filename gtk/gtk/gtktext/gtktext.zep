// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace Gtk\Gtk\GtkText;

class GtkText
{
    public static function new_() -> int
    {
        return gtktext_new();
    }

    public static function newWithBuffer(int buffer) -> int
    {
        return gtktext_new_with_buffer(buffer);
    }

    public static function computeCursorExtents(int handle, int position) -> array
    {
        var result;

        let result = gtktext_compute_cursor_extents(handle, position);

        return result;
    }

    public static function getActivatesDefault(int handle) -> bool
    {
        int r;

        let r = gtktext_get_activates_default(handle);

        return r == 1;
    }

    public static function getBuffer(int handle) -> int
    {
        return gtktext_get_buffer(handle);
    }

    public static function getEnableEmojiCompletion(int handle) -> bool
    {
        int r;

        let r = gtktext_get_enable_emoji_completion(handle);

        return r == 1;
    }

    public static function getExtraMenu(int handle) -> int
    {
        return gtktext_get_extra_menu(handle);
    }

    public static function getInputHints(int handle) -> int
    {
        return gtktext_get_input_hints(handle);
    }

    public static function getInputPurpose(int handle) -> int
    {
        return gtktext_get_input_purpose(handle);
    }

    public static function getInvisibleChar(int handle) -> int
    {
        return gtktext_get_invisible_char(handle);
    }

    public static function getMaxLength(int handle) -> int
    {
        return gtktext_get_max_length(handle);
    }

    public static function getOverwriteMode(int handle) -> bool
    {
        int r;

        let r = gtktext_get_overwrite_mode(handle);

        return r == 1;
    }

    public static function getPlaceholderText(int handle) -> var
    {
        var result;

        let result = gtktext_get_placeholder_text(handle);

        return result;
    }

    public static function getPropagateTextWidth(int handle) -> bool
    {
        int r;

        let r = gtktext_get_propagate_text_width(handle);

        return r == 1;
    }

    public static function getTextLength(int handle) -> int
    {
        return gtktext_get_text_length(handle);
    }

    public static function getTruncateMultiline(int handle) -> bool
    {
        int r;

        let r = gtktext_get_truncate_multiline(handle);

        return r == 1;
    }

    public static function getVisibility(int handle) -> bool
    {
        int r;

        let r = gtktext_get_visibility(handle);

        return r == 1;
    }

    public static function grabFocusWithoutSelecting(int handle) -> bool
    {
        int r;

        let r = gtktext_grab_focus_without_selecting(handle);

        return r == 1;
    }

    public static function setActivatesDefault(int handle, bool activates) -> void
    {
        gtktext_set_activates_default(handle, activates);
    }

    public static function setBuffer(int handle, int buffer) -> void
    {
        gtktext_set_buffer(handle, buffer);
    }

    public static function setEnableEmojiCompletion(int handle, bool enableEmojiCompletion) -> void
    {
        gtktext_set_enable_emoji_completion(handle, enableEmojiCompletion);
    }

    public static function setExtraMenu(int handle, int model) -> void
    {
        gtktext_set_extra_menu(handle, model);
    }

    public static function setInputHints(int handle, int hints) -> void
    {
        gtktext_set_input_hints(handle, hints);
    }

    public static function setInputPurpose(int handle, int purpose) -> void
    {
        gtktext_set_input_purpose(handle, purpose);
    }

    public static function setInvisibleChar(int handle, int ch) -> void
    {
        gtktext_set_invisible_char(handle, ch);
    }

    public static function setMaxLength(int handle, int length) -> void
    {
        gtktext_set_max_length(handle, length);
    }

    public static function setOverwriteMode(int handle, bool overwrite) -> void
    {
        gtktext_set_overwrite_mode(handle, overwrite);
    }

    public static function setPlaceholderText(int handle, var text) -> void
    {
        gtktext_set_placeholder_text(handle, text);
    }

    public static function setPropagateTextWidth(int handle, bool propagateTextWidth) -> void
    {
        gtktext_set_propagate_text_width(handle, propagateTextWidth);
    }

    public static function setTruncateMultiline(int handle, bool truncateMultiline) -> void
    {
        gtktext_set_truncate_multiline(handle, truncateMultiline);
    }

    public static function setVisibility(int handle, bool visible) -> void
    {
        gtktext_set_visibility(handle, visible);
    }

    public static function unsetInvisibleChar(int handle) -> void
    {
        gtktext_unset_invisible_char(handle);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved property "im-module" — no C accessor; use Bridge::getProperty/setProperty
    // @reserved property "invisible-char-set" — no C accessor; use Bridge::getProperty/setProperty
    // @reserved property "scroll-offset" — no C accessor; use Bridge::getProperty
    // @reserved gtk_text_get_attributes(self) — PangoAttrList*
    // @reserved gtk_text_get_tabs(self) — PangoTabArray*
    // @reserved gtk_text_set_attributes(self, PangoAttrList* attrs) — PangoAttrList*
    // @reserved gtk_text_set_tabs(self, PangoTabArray* tabs) — PangoTabArray*

}
