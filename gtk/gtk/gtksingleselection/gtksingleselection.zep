// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace Gtk\Gtk\GtkSingleSelection;

class GtkSingleSelection
{
    public static function new_(int model) -> int
    {
        return gtksingleselection_new(model);
    }

    public static function getAutoselect(int handle) -> bool
    {
        int r;

        let r = gtksingleselection_get_autoselect(handle);

        return r == 1;
    }

    public static function getCanUnselect(int handle) -> bool
    {
        int r;

        let r = gtksingleselection_get_can_unselect(handle);

        return r == 1;
    }

    public static function getModel(int handle) -> int
    {
        return gtksingleselection_get_model(handle);
    }

    public static function getSelected(int handle) -> int
    {
        return gtksingleselection_get_selected(handle);
    }

    public static function getSelectedItem(int handle) -> int
    {
        return gtksingleselection_get_selected_item(handle);
    }

    public static function setAutoselect(int handle, bool autoselect) -> void
    {
        gtksingleselection_set_autoselect(handle, autoselect);
    }

    public static function setCanUnselect(int handle, bool canUnselect) -> void
    {
        gtksingleselection_set_can_unselect(handle, canUnselect);
    }

    public static function setModel(int handle, int model) -> void
    {
        gtksingleselection_set_model(handle, model);
    }

    public static function setSelected(int handle, int position) -> void
    {
        gtksingleselection_set_selected(handle, position);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved property "item-type" — getter lives on GListModel; use Bridge::getProperty
    // @reserved property "n-items" — getter lives on GListModel; use Bridge::getProperty

}
